---
driver:
  name: terraform
  root_module_directory: test/fixtures
  parallelism: 4

provisioner:
  name: terraform

platforms:
  - name: azure

suites:
  - name: single-account
    driver:
      root_module_directory: test/fixtures/single-project
